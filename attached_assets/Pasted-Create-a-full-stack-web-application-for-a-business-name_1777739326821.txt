Create a full-stack web application for a business named "Global Enterprise".

BUSINESS SERVICES:

* Air Ticket Booking
* Train Ticket Booking
* International Parcel Booking
* PAN Card Apply
* Aadhaar Card Services
* Voter Card Apply

TECH STACK:

* Frontend: HTML, CSS, JavaScript (responsive UI)
* Backend: Node.js with Express
* Database: PostgreSQL (Neon DB)
* Use environment variables for DB connection

DESIGN:

* Clean modern UI (Blue + White theme)
* Mobile responsive
* Use Font Awesome icons

FEATURES:

1. HOME PAGE:

* Business intro
* Service cards
* WhatsApp button (dynamic message)

2. SERVICES PAGE:

* List all services
* "Apply Now" button

3. APPLY FORM:
   Fields:

* Full Name
* Phone
* Service Type (dropdown)
* Message

On submit:

* Save to PostgreSQL (Neon)
* Show success message

4. ADMIN PANEL:

* Login system (JWT based)
* Dashboard:

  * Total applications count
  * Recent submissions
* Table view:

  * Name, Phone, Service, Message, Date
* Filter by service
* Export CSV

5. USER TRACKING:

* Count visitors (store only count, no heavy logs)

6. CONTACT PAGE:

* Phone number
* WhatsApp button
* Google Map embed
* Address

7. WHATSAPP INTEGRATION:
   Dynamic link:
   https://wa.me/<number>?text=Hello Global Enterprise, I want [service]

DATABASE (PostgreSQL - Neon):

Tables:

users (admin login)

* id (primary key)
* username
* password (hashed)

applications

* id
* name
* phone
* service
* message
* created_at

visitors

* id
* visit_count

Use pg or Prisma ORM.

ENV VARIABLES:
DATABASE_URL=Neon connection string
JWT_SECRET=your_secret

ADMIN DEFAULT:
username: admin
password: admin123

SECURITY:

* Hash passwords (bcrypt)
* Validate inputs
* Protect admin routes

FOLDER STRUCTURE:

* /public (frontend)
* /server (backend)
* /routes
* /models

FLY.IO DEPLOYMENT:

* Add Dockerfile
* Use PORT from environment
* Expose app on 0.0.0.0
* Include fly.toml sample
* Add steps:
  fly launch
  fly deploy

NEON SETUP:

* Use connection string
* Auto create tables if not exist

OUTPUT:

* Full working code
* Ready to deploy on Fly.io
* Comments in code


You may improve UI/UX, layout, and code quality using your own best practices, 
but DO NOT change core features, business logic, database structure, or service list.
Keep everything aligned with the purpose of "Global Enterprise".